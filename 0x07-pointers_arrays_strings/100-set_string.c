#include "main.h"
#include <stdio.h>

/**
 * set_string - a function that will store the value of
 * a pointer to char
 * @s: the double pointer
 * @to: the pointer
 *
 * Description: refer to the first commented out line
 */
void set_string(char **s, char *to)
{
	*s = to;
}
